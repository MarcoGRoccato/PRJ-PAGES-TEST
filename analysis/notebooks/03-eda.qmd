---
title: "03 — EDA"
execute:
  echo: true
  warning: false
---

## Goals
Explore distributions, relationships, outliers, and missingness in the **cleaned** data.

### Load data
```{r}
# R (optional)
# library(readr); library(dplyr); library(ggplot2)
# df <- readr::read_csv("data/interim/clean.csv")
# glimpse(df)
